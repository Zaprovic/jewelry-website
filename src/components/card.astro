---
// import {Image} from "astro:assets"

interface Props {
    imageName: string;
    description: string;
    name?: string;
    category?: string;
    price?:string;
    index?:number
}


const {imageName,description, name, category, price,index} = Astro.props;
---


<div class="card">

    <figure>
        <img src={imageName} alt={description}/>
    </figure>

    <div class="second-col">
        <div class="titles">
            <h3>{category}</h3>
            <h4>{name} - {index}</h4>
            <h5>$ {price}</h5>
        </div>
    
        <p>
            Lorem ipsum dolor sit amet, consectetur adipisicing elit. Laboriosam iure, dolores quae maxime vero esse repudiandae sed quidem, nihil perspiciatis minus ipsam! Facilis voluptates iste quaerat aliquam et cum optio.
        </p>   
    </div>

</div>

<style>

    .card {
        display: flex;
        flex-direction: column;
        font-size: 2.4rem;
        background: rgb(223, 223, 223);
        margin: 4rem;
        /* padding: 2.8rem; */
        border-radius: 2.8rem;
        transition: all ease 0.3s;
        align-items: center;
        overflow: hidden;
    }
    .card figure {
        /* padding: 2rem; */
        width: 100%;
        margin-bottom: 4rem;
    }


    .card:hover{
        transform: scale(0.98);
    }

    .card figure img {
        width: 100%;
        height: 260px;
        object-fit: cover;
    }

    .card .second-col {
        display: flex;
        flex-direction: column;
        gap: 3.6rem;
        padding: 0 3.6rem;
    }


    .titles {
        display: flex;
        flex-direction: column;
        gap: 1.2rem;
    }

    h3 {
        font-size: 3.2rem;
        color: gray;
    }

    h4 {
        font-size: 3.6rem;
        font-weight: bold;
    }

    h5 {
        font-size: 4.8rem;
    }

    p {
        text-align: justify;
        margin-bottom: 4rem;
        font-size: 2.8rem;
        line-height: 1.6;
    }

</style>